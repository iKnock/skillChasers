version: "3"
services:
  mongodb:
    image: "mongo:latest"
    ports:
      - "27017:27017"
  skillchasers-app:
    depends_on:
      - mongodb
    restart: on-failure
    build: .
    ports:
      - "5000:5000"
